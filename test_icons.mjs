import * as Si from 'react-icons/si';
import * as Fa from 'react-icons/fa';
import * as Fa6 from 'react-icons/fa6';
import * as Tb from 'react-icons/tb';

console.log("Si AWS keys:", Object.keys(Si).filter(k => k.toLowerCase().includes('aws')));
console.log("Fa AWS keys:", Object.keys(Fa).filter(k => k.toLowerCase().includes('aws')));
console.log("Fa6 AWS keys:", Object.keys(Fa6).filter(k => k.toLowerCase().includes('aws')));
console.log("Tb AWS keys:", Object.keys(Tb).filter(k => k.toLowerCase().includes('aws')));
